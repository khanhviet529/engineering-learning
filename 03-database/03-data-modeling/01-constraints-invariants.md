# Constraints & invariants

**Position:** Domain → Database

Các invariant quan trọng nên được bảo vệ ở nơi phù hợp, thường bao gồm DB constraints: NOT NULL, UNIQUE, FK, CHECK. Application validation không thay thế hoàn toàn integrity của database.
