# Relational thinking

**Position:** Application → SQL database

Tập nghĩ bằng tập hợp, quan hệ, constraints và invariants thay vì chỉ CRUD từng row. Học SELECT/JOIN/GROUP BY/subquery qua câu hỏi dữ liệu thật.
