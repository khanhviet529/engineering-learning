# JOIN & aggregation

**Position:** SQL query layer

Experiment với 1-N, N-N, missing rows; so INNER/LEFT JOIN; kiểm tra duplicate rows sau join; dùng GROUP BY đúng grain.

Câu hỏi quan trọng: "mỗi row của kết quả đại diện cho cái gì?"
