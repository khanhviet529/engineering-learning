# Requirements before architecture

System design bắt đầu từ workload và constraints: users, read/write ratio, data size, latency target, availability, consistency, security, cost và growth.

Không chọn Redis/Kafka/K8s trước khi biết problem cần giải quyết.
