# Config, secrets & resources

**Position:** Kubernetes → Container runtime

Tách config khỏi image; bảo vệ secrets phù hợp; đặt requests/limits dựa trên quan sát thay vì copy mặc định. Resource constraints ảnh hưởng scheduling và runtime behavior.
