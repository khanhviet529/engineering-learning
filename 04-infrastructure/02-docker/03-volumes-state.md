# Volumes & state

**Position:** Container ↔ Persistent data

Container có thể bị thay thế; dữ liệu cần persistence phải được tách khỏi writable layer phù du. Phân biệt bind mount và managed volume theo use case.
