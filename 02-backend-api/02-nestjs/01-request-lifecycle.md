# NestJS request lifecycle

**Position:** HTTP → NestJS → Service

Tạo mental map về middleware, guards, interceptors, pipes, controller/service và exception handling. Mục tiêu không phải nhớ thứ tự bằng thuộc lòng mà trace được một request thật.

Experiment: thêm log ở từng tầng và gửi request hợp lệ/không hợp lệ/không auth.
