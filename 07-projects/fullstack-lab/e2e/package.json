{
  "name": "@flowboard/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "End-to-end qua browser trên stack Compose thật. Không mock, không seed: dữ liệu được tạo qua chính sản phẩm.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@flowboard/config": "workspace:*",
    "@playwright/test": "^1.57.0",
    "@types/node": "^26.4.1"
  }
}
